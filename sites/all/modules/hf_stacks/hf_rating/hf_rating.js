(function($) {
	// disable all radio buttons
	/*
	$.fn.hf_rating_disable = function(data) {
		$( this ).prop( "disabled", true );
	};
	*/

	/*
	// enable checkboxes
	$.fn.hf_rating_enable = function(data) {
		$( this ).prop( "disabled", false );
	};

	// increment button
	$.fn.hf_rating_plus = function(data) {
		var val = $(this).val();
		$(this).val((val*1)+1);
	};

	// decrement button
	$.fn.hf_rating_minus = function(data) {
		var val = $(this).val();
		if  (val >= 1) {
			$(this).val((val*1)-1);
		}
	};
	*/
})(jQuery);